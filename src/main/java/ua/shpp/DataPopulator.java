package ua.shpp;

import java.io.InputStream;

public class DataPopulator {
    public DataPopulator(DbRepository dbRepository, AppConfig config) {
    }

    public void fillInTables(InputStream dbRepository, InputStream config) {
    }
}
